{
  "name": "agentbridge",
  "version": "0.1.0",
  "description": "Write AI agent plugins once, ship to any harness. Unifying library for Claude Code, Codex, OpenCode, Gemini, Copilot CLI, Kimi, and Pi Mono.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean",
    "release": "pnpm build && changeset publish",
    "version": "changeset version"
  },
  "keywords": [
    "ai",
    "agent",
    "plugin",
    "claude",
    "codex",
    "copilot",
    "gemini",
    "kimi",
    "opencode",
    "pimono",
    "unplugin"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "pnpm@9.0.0",
  "devDependencies": {
    "@changesets/cli": "^2.31.0"
  }
}
